services:
  duck-flow:
    build:
      context: .
      target: develop-stage
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    # 确保 Vite 在 Docker 内正确处理 HMR 热更新
    command: npm run dev -- --host 0.0.0.0
